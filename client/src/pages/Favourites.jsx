// Import Required modules
import React from "react";

// An Favourites page for the App
const Favourites = () => {
    
    // JSX to render the page
    return (
        <main className="h-full p-2 border border-cyan-400">Favourites</main>
    );
};

// Export the Favourites page
export default Favourites;