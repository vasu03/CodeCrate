// Import required modules
import React from "react";

// A Footer component for the App
export const Footer = () => {

    // JSX to render the component
    return (
        <footer className="border border-green-500 p-2">
            Footer
        </footer>
    );
};